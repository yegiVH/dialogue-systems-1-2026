declare class EventAsPromise<T> {
    #private;
    constructor();
    get eventListener(): (event: T) => void;
    [Symbol.iterator](): {
        next: () => {
            done: boolean;
            value: Promise<T>;
        };
    };
    one(): Promise<T>;
    upcoming(): Promise<T>;
}

export { EventAsPromise };
